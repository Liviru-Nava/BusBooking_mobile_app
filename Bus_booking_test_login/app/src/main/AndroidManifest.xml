<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bus_booking_test_login"
        tools:targetApi="31">
        <activity android:name=".ActivityRegisterSelect" />
        <activity android:name=".ActivityRegisterPassenger"/>
        <activity android:name=".ActivityPassengerHome"/>
        <activity android:name=".ActivityRegisterBusOwner"/>
        <activity android:name=".ActivityRegisterBusAndRoute"/>
        <activity android:name=".ActivityRegisterDriver"/>
        <activity android:name=".ActivityBusList"/>
        <activity android:name=".ActivityBookSeat"/>
        <activity android:name=".ActivityPayment"/>
        <activity android:name=".ActivityBookingList"/>
        <activity android:name=".ActivityLogin" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>